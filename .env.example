# Supply secrets only during deployment. Do not paste actual values into prompts.
OPENAI_API_KEY=
ESPN_LEAGUE_URL=https://lm-api-reads.fantasy.espn.com/apis/v3/games/ffl/seasons/2026/segments/0/leagues/125290435?view=mTeam&view=mRoster
# The reader/proxy owns these secrets; never pass them into model context.
ESPN_S2=
ESPN_SWID=
# ESPN's browser-cookie spellings are also accepted: espn_s2 and SWID.
# Delivery remains disabled until the Discord transport dry run passes.
DISCORD_WEBHOOK=
DELIVERY_ENABLED=false
# Set this only for offline verification; it prevents model calls even if a key exists.
FANTASY_DISABLE_LLM=false
